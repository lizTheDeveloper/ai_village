--- a/packages/core/src/systems/FederationGovernanceSystem.ts
+++ b/packages/core/src/systems/FederationGovernanceSystem.ts
@@ -237,6 +237,13 @@ export class FederationGovernanceSystem extends BaseSystem {
    * Calculate aggregate statistics for member states
    */
   private aggregateMemberStatistics(
+    // OPTIMIZATION: Pass cached entities instead of querying
+    allEmpires: ReadonlyArray<Entity>,
+    allNations: ReadonlyArray<Entity>,
     world: World,
     federation: FederationGovernanceComponent
   ): MemberStats {
@@ -244,9 +251,7 @@ export class FederationGovernanceSystem extends BaseSystem {
     let totalMilitary = 0;
     let totalTerritory = 0;

-    // ❌ BEFORE: Query inside loop
-    const allEmpires = world.query().with(CT.Empire).executeEntities();
-
+    // ✅ AFTER: Use passed cached entities
     for (const empireId of federation.memberEmpireIds) {
       const empireEntity = allEmpires.find((e) => e.id === empireId);
       if (!empireEntity) continue;
@@ -326,8 +331,7 @@ export class FederationGovernanceSystem extends BaseSystem {
    * Calculate weighted voting power for member states
    */
   private calculateVotingPower(
+    allEmpires: ReadonlyArray<Entity>,
     world: World,
     federation: FederationGovernanceComponent
   ): void {
-    // ❌ BEFORE: Query repeated
-    const allEmpires = world.query().with(CT.Empire).executeEntities();
+    // ✅ AFTER: Use passed cached entities

     for (const vote of federation.assemblyVotes) {
       const empireEntity = allEmpires.find((e) => e.id === vote.memberStateId);
@@ -417,8 +421,7 @@ export class FederationGovernanceSystem extends BaseSystem {
    * Coordinate military defense across member states
    */
   private coordinateDefense(
+    allNavies: ReadonlyArray<Entity>,
     world: World,
     federation: FederationGovernanceComponent
   ): void {
-    // ❌ BEFORE: Query repeated
-    const allNavies = world.query().with(CT.Navy).executeEntities();
+    // ✅ AFTER: Use passed cached entities

     for (const navyEntity of allNavies) {
       const navy = navyEntity.getComponent<NavyComponent>(CT.Navy);
@@ -489,8 +492,7 @@ export class FederationGovernanceSystem extends BaseSystem {
    * Allocate diplomatic resources
    */
   private allocateDiplomaticResources(
+    allEmpires: ReadonlyArray<Entity>,
     world: World,
     federation: FederationGovernanceComponent
   ): void {
-    // ❌ BEFORE: Query repeated
-    const allEmpires = world.query().with(CT.Empire).executeEntities();
+    // ✅ AFTER: Use passed cached entities

     for (const empireId of federation.memberEmpireIds) {
       // ...
@@ -600,8 +602,7 @@ export class FederationGovernanceSystem extends BaseSystem {
    * Process trade agreements
    */
   private processTradeAgreements(
+    allEmpires: ReadonlyArray<Entity>,
     world: World,
     federation: FederationGovernanceComponent
   ): void {
-    // ❌ BEFORE: Query repeated
-    const allEmpires = world.query().with(CT.Empire).executeEntities();
+    // ✅ AFTER: Use passed cached entities

     for (const agreement of federation.trade.agreements) {
       // ...
@@ -892,8 +893,7 @@ export class FederationGovernanceSystem extends BaseSystem {
    * Calculate overall military readiness
    */
   private calculateOverallReadiness(
+    allNavies: ReadonlyArray<Entity>,
     world: World,
     federation: FederationGovernanceComponent
   ): number {
-    // ❌ BEFORE: Query repeated
-    const allNavies = world.query().with(CT.Navy).executeEntities();
+    // ✅ AFTER: Use passed cached entities

     let totalReadiness = 0;
     let navyCount = 0;
@@ -143,6 +143,12 @@ export class FederationGovernanceSystem extends BaseSystem {
   ): void {
     const federation = federationEntity.getComponent<FederationGovernanceComponent>(CT.Federation);
     if (!federation) return;
+
+    // OPTIMIZATION: Cache all queries before processing
+    const allEmpires = world.query().with(CT.Empire).executeEntities();
+    const allNations = world.query().with(CT.Nation).executeEntities();
+    const allNavies = world.query().with(CT.Navy).executeEntities();

     // Step 1: Aggregate member statistics
-    const memberStats = this.aggregateMemberStatistics(world, federation);
+    const memberStats = this.aggregateMemberStatistics(allEmpires, allNations, world, federation);

     // Step 2: Calculate voting power
-    this.calculateVotingPower(world, federation);
+    this.calculateVotingPower(allEmpires, world, federation);

     // Step 3: Process proposals
     this.processProposals(world, federation, tick);

     // Step 4: Coordinate defense
-    this.coordinateDefense(world, federation);
+    this.coordinateDefense(allNavies, world, federation);

     // Step 5: Allocate resources
-    this.allocateDiplomaticResources(world, federation);
+    this.allocateDiplomaticResources(allEmpires, world, federation);

     // Step 6: Process trade
-    this.processTradeAgreements(world, federation);
+    this.processTradeAgreements(allEmpires, world, federation);

     // Step 7: Calculate readiness
-    const readiness = this.calculateOverallReadiness(world, federation);
+    const readiness = this.calculateOverallReadiness(allNavies, world, federation);

     // Update component
     federationEntity.updateComponent<FederationGovernanceComponent>(CT.Federation, (f) => ({
